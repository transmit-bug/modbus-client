[env]
# ts-rs exports TypeScript bindings here (relative to the workspace root)
# whenever `cargo test` runs the generated export tests.
TS_RS_EXPORT_DIR = { value = "frontend/src/types", relative = true }
